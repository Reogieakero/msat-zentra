import { AdmBoard } from "./AdmBoard";

export default function PrincipalAdmPage() {
  return (
    <AdmBoard
      title="ADM Referrals & Approvals"
      description="Alternate Delivery Mode learner profiles — final-sign referrals prepared by the ADM Coordinator."
    />
  );
}
