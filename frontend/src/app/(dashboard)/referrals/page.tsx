import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

export default function ReferralsPage() {
  return (
    <ScaffoldPlaceholder
      title="Referrals"
      description="Referral state machine: anecdotal → guidance/nurse/ADM/principal."
      moduleId="referrals"
    />
  );
}
