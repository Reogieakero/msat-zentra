import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

export default function NotificationsPage() {
  return (
    <ScaffoldPlaceholder
      title="Notifications"
      description="Cross-module notification inbox (web/mobile/email channels)."
      moduleId="notifications"
    />
  );
}
