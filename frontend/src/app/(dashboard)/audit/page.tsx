import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

export default function AuditPage() {
  return (
    <ScaffoldPlaceholder
      title="Audit Log"
      description="School-wide audit trail of sensitive actions (Principal only)."
      moduleId="audit"
    />
  );
}
