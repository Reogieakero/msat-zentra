import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

export default function AdmPage() {
  return (
    <ScaffoldPlaceholder
      title="ADM"
      description="Learner profiles, eligibility, modules, and device tracking."
      moduleId="adm"
    />
  );
}
