import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

export default function GradesPage() {
  return (
    <ScaffoldPlaceholder
      title="Grades"
      description="Encode scores, compute finals, lock for registrar approval."
      moduleId="grades"
    />
  );
}
