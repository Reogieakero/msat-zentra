---
description: Apply Zentra's anti-generic frontend design language (Tailwind + shadcn/ui custom tokens) to a screen, component, or UI task
---

Apply the Zentra frontend design language to the user's request below.

1. Load `docs/frontend-design-direction.md` and the relevant role spec in
   `docs/role-modules/` (ask which role if not stated).
2. Enforce the anti-generic rules: soft off-white `#FAFAF9` background, ONE brand
   accent (no multi-color gradients, no gradient text), `rounded-md` shape,
   1px borders over heavy shadows, display+body font pairing (Geist/Space Grotesk
   + Inter), role-aware density, micro-motion only (120–180ms ease-out).
3. Use shadcn/ui + Tailwind components. Never produce: centered gradient-blob hero,
   rainbow sidebar, over-rounded pills, decorative blobs, "✨ powered by AI" badges,
   generic robot SVGs, default Unsplash heros, pure-white backgrounds.
4. Cite which role/module page the UI targets and which design tokens were applied.

User request:
$ARGUMENTS
