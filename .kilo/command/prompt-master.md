---
description: Generate an optimized, paste-ready prompt for any AI tool (LLM, Cursor, Midjourney, image/video AI, coding agents, etc.)
---

Activate the **prompt-master** skill and run its full pipeline on the user's request below.

Treat the user's input as a request to write, fix, improve, or adapt a prompt for a specific AI tool. If the target tool is ambiguous, ask up to 3 clarifying questions first. Then output a single copyable prompt block optimized for that tool, plus the 🎯 target / 💡 strategy line.

User request:
$ARGUMENTS
